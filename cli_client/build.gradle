dependencies {
    compile project(':client')
}

jar {
    manifest {
        attributes (
                'Main-class': 'ru.spbau.gusev.ftp.cli_client.ClientCLI'
        )
    }
    from {
        configurations.compile.collect {
            it.isDirectory() ? it : zipTree(it)
        }
    }
}